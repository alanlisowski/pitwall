# PitWall — Web Front-end

React + TypeScript + Vite front-end. Not yet scaffolded.

To be initialised with:

```bash
npm create vite@latest . -- --template react-ts
npm install
npm run dev
```

Dependencies planned: Tailwind CSS, Recharts.
